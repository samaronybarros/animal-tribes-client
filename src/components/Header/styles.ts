import { Navbar as MaterilizedNavbar } from 'react-materialize'
import styled from 'styled-components'

export const StyledNavbar = styled(MaterilizedNavbar)`
  background-color: #141414;
  padding: 0 20px;
`

export const Logo = styled.a`
  font-family: 'Hanalei Fill', cursive;
`
